# Frontend
React app for the Mini Lead CRM. Full documentation, setup, and design notes are in the **[root README](../README.md)**.

## Quick run

```bash
npm install
npm run dev
```

Requires the mock API running — see root README.

Optional: `frontend/.env` with `VITE_API_URL=http://localhost:4000` if the API uses a different host or port.
